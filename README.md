# kelomit-app
My time-stalker for workdays

## Development

This repo uses npm.

```sh
npm install
npm start
npm run android
```

Run all local checks with:

```sh
npm run check
```

For Android builds, configure the Android SDK with `ANDROID_HOME` or an
`android/local.properties` file that points at your local SDK.
