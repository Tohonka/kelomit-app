<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Opt-in background GPS: keep logging location while the app is closed so
         work enter/exit are detected at the real time. Runs only behind the
         user's "Track in background" setting + the "Allow all the time" grant,
         via a location-typed foreground service (LocationService). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Fast wake from a parked saved-place: detect the user starting to move
         (walking/cycling/vehicle) before the geofence exit fires. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact-alarm scheduling for a timed tracking-pause auto-resume: an
         inexact alarm only temp-allowlists the app, not the
         foreground-service-start privilege, so on Android 12+ the resume
         alarm could fire and then silently fail to restart tracking while
         the app is closed. Falls back to an inexact alarm if not granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Media-library / external-storage permissions intentionally trimmed
         (Iteration 3 Phase 2): gallery imports use the Android system photo
         picker (per-item access, no permission) and all app media is captured
         to app-scoped storage.
         READ_MEDIA_IMAGES/VIDEO/AUDIO are simply not declared.
         READ/WRITE_EXTERNAL_STORAGE ARE injected by transitive deps
         (audio/media libs + react-native-fs, uncapped), so
         they must be force-removed from the merged manifest below. Re-add a
         specific permission only if a feature genuinely needs shared storage,
         with a comment explaining why. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- Google Maps SDK key, injected from .maps.env via the MAPS_API_KEY
           manifest placeholder (android/app/build.gradle). Never hard-coded. -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="${MAPS_API_KEY}" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- kelomit://quickadd/<note|photo|voice> — home-screen add-note widget
             opens straight into quick-add (Task 1 of the widget iteration). -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="kelomit" />
        </intent-filter>
      </activity>

      <!-- Home-screen session widgets (Iteration 3 Phase 9.2). ACTION_TOGGLE is
           our own start/stop broadcast; APPWIDGET_UPDATE is the system's. -->
      <receiver
        android:name=".widget.SessionWidgetProvider"
        android:exported="false">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          <action android:name="com.kelomitapp.widget.ACTION_TOGGLE" />
          <action android:name="com.kelomitapp.widget.ACTION_PAUSE_RESUME" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/widget_session_info" />
      </receiver>

      <receiver
        android:name=".widget.SessionToggleWidgetProvider"
        android:exported="false">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          <action android:name="com.kelomitapp.widget.ACTION_TOGGLE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/widget_session_toggle_info" />
      </receiver>

      <receiver
        android:name=".widget.AddNoteWidgetProvider"
        android:exported="false">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/widget_add_note_info" />
      </receiver>

      <!-- Opt-in background GPS keep-alive service (location-typed). -->
      <service
        android:name=".location.LocationService"
        android:exported="false"
        android:foregroundServiceType="location" />

      <!-- Always-on saved-place monitoring for work arrive/leave detection. -->
      <receiver
        android:name=".location.GeofenceCrossingReceiver"
        android:exported="false" />

      <receiver
        android:name=".location.PlaceBootReceiver"
        android:exported="false">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        </intent-filter>
      </receiver>

      <receiver
        android:name=".location.DayEndAlarmReceiver"
        android:exported="false" />

      <receiver
        android:name=".location.DayEndActionReceiver"
        android:exported="false" />

      <!-- AlarmManager target for a timed tracking pause elapsing. -->
      <receiver
        android:name=".location.TrackingResumeReceiver"
        android:exported="false" />

      <!-- Stop-tracking widget: pause movement detection for 1h/4h/until
           resumed, or resume it. -->
      <receiver
        android:name=".location.TrackingPauseWidgetProvider"
        android:exported="false">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
          <action android:name="com.kelomitapp.widget.ACTION_TRACKING_PAUSE_1H" />
          <action android:name="com.kelomitapp.widget.ACTION_TRACKING_PAUSE_4H" />
          <action android:name="com.kelomitapp.widget.ACTION_TRACKING_PAUSE_INF" />
          <action android:name="com.kelomitapp.widget.ACTION_TRACKING_RESUME" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/widget_tracking_pause_info" />
      </receiver>

      <!-- Wakes the idle location service the moment activity-recognition
           reports the user started moving (faster than the geofence exit). -->
      <receiver
        android:name=".location.ActivityTransitionReceiver"
        android:exported="false" />
    </application>
</manifest>
